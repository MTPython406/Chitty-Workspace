{
  "name": "gcloud_bigquery",
  "display_name": "BigQuery",
  "description": "Query datasets, create tables, and manage data in Google BigQuery",
  "version": "1.0.0",
  "tool_type": "custom",
  "runtime": "python",
  "entry_point": "tool.py",
  "parameters": {
    "action": {
      "type": "string",
      "description": "The BigQuery action to perform: list_datasets, create_dataset, delete_dataset, list_tables, describe_table, query, insert_rows",
      "required": true
    },
    "project_id": {
      "type": "string",
      "description": "GCP project ID. If omitted, uses the default gcloud project.",
      "required": false
    },
    "dataset_id": {
      "type": "string",
      "description": "BigQuery dataset ID (required for dataset/table operations)",
      "required": false
    },
    "table_id": {
      "type": "string",
      "description": "BigQuery table ID (required for table operations)",
      "required": false
    },
    "sql": {
      "type": "string",
      "description": "SQL query to execute (required for 'query' action)",
      "required": false
    },
    "rows": {
      "type": "array",
      "description": "JSON array of row objects to insert (required for 'insert_rows' action)",
      "required": false
    },
    "location": {
      "type": "string",
      "description": "Dataset location (default: US)",
      "required": false
    }
  },
  "install_commands": [
    "pip install google-cloud-bigquery google-auth"
  ],
  "timeout_seconds": 60,
  "permission_tier": "moderate",
  "source": "marketplace",
  "marketplace_id": "google-cloud",
  "instructions": "Use this tool to interact with Google BigQuery. The user authenticates via their own gcloud CLI login.\n\n**Available actions:**\n- `list_datasets` — List all datasets in a project\n- `create_dataset` — Create a new dataset (requires dataset_id, optional location)\n- `delete_dataset` — Delete a dataset (requires dataset_id)\n- `list_tables` — List tables in a dataset (requires dataset_id)\n- `describe_table` — Get table schema (requires dataset_id + table_id)\n- `query` — Run a SQL query (requires sql). Returns up to 100 rows as JSON.\n- `insert_rows` — Insert rows into a table (requires dataset_id + table_id + rows)\n\n**Important:** Always check if a project_id is needed. If the user hasn't specified one, omit it to use their default gcloud project."
}
